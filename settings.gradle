pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'ushort'

toolchainManagement {
    jvm {
        javaRepositories {
            repository("adoptium") {
                resolverClass = org.gradle.jvm.toolchain.internal.install.AdoptiumResolver
            }
        }
    }
}
